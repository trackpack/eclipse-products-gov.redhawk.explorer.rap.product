<?xml version="1.0" encoding="UTF-8"?>
<project
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
  xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>gov.redhawk.explorer.rap.product</groupId>
    <artifactId>releng</artifactId>
    <version>1.10.0-SNAPSHOT</version>
    <relativePath>../../releng/</relativePath>
  </parent>

  <groupId>gov.redhawk.explorer.rap.product</groupId>
  <artifactId>gov.redhawk.rap.platform</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>eclipse-feature</packaging>

  <properties>
  	<output-directory>${basedir}/../../releng/product/target/WEB-INF</output-directory>
  </properties>
<!--
  <build>
	<plugins>
		<plugin>
			<artifactId>maven-resources-plugin</artifactId>
			<version>2.4.3</version>
			<executions>
				<execution>
					<id>copy-web-inf-and-target-plugins</id>
					<phase>deploy</phase>
					<goals>
						<goal>copy-resources</goal>
					</goals>
					<configuration>
						<outputDirectory>${output-directory}</outputDirectory>
						<resources>
							<resource>
								<directory>${basedir}/../../releng/product/target/products/gov.redhawk.explorer.rap.product/linux/gtk/x86_64/WEB-INF</directory>
								<includes>
									<include>*/**</include>
								</includes>
								<excludes>
									<exclude>plugins</exclude>
									<exclude>features</exclude>
									<exclude>web.xml</exclude>
								</excludes>
							</resource>
						</resources>
					</configuration>
				</execution>
				<execution>
					<id>copy-plugins</id>
					<phase>install</phase>
					<goals>
						<goal>copy-resources</goal>
					</goals>
					<configuration>
						<outputDirectory>${output-directory}</outputDirectory>
						<resources>
							<resource>
								<directory>${basedir}/target/site</directory>
								<includes>
									<include>*/**</include>
								</includes>
								<excludes>
									<exclude>content.jar</exclude>
									<exclude>artifacts.jar</exclude>
									<exclude>*/javax.servlet*</exclude>
									<exclude>*/web.xml</exclude>
								</excludes>
							</resource>
						</resources>
					</configuration>
				</execution>
			</executions>
		</plugin>
	</plugins>
  </build>
-->
</project>
